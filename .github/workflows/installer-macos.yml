on:
  pull_request:
  release:
    types:
      - created

name: Create macOS App Bundle and DMG

jobs:
  build:
    name: macOS App Bundle
    runs-on: macos-latest
    strategy:
      matrix:
        build_type: ['Full']
    defaults:
      run:
        shell: bash -l {0}
        working-directory: ${{ github.workspace }}/installers/macOS
    env:
      LITE_FLAG: ${{ matrix.build_type == 'Lite' && '--lite' || '' }}
      DMGNAME: ${{ matrix.build_type == 'Lite' && 'Spyder-Lite.dmg' || 'Spyder.dmg' }}
      DISTDIR: ${{ github.workspace }}/dist
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          # This is the last version working with py2app and modulegraph
          python-version: '3.9.5'
          architecture: 'x64'
      - name: Install Dependencies
        run: |
          INSTALL_FLAGS=()
          if [[ -z ${LITE_FLAG} ]]; then
              INSTALL_FLAGS+=('-r' 'req-scientific.txt')
          fi
          ${pythonLocation}/bin/python -m pip install -U pip setuptools
          ${pythonLocation}/bin/python -m pip install -r req-build.txt -r req-extras.txt "${INSTALL_FLAGS[@]}" -e ${GITHUB_WORKSPACE}
          ${pythonLocation}/bin/python -m pip uninstall -q -y spyder
      - name: Install Subrepos
        if: ${{github.event_name == 'pull_request'}}
        run: |
          ${pythonLocation}/bin/python -m pip install \
            -e ${GITHUB_WORKSPACE}/external-deps/spyder-kernels \
            -e ${GITHUB_WORKSPACE}/external-deps/python-lsp-server \
            -e ${GITHUB_WORKSPACE}/external-deps/qdarkstyle
      - name: Show Build Environment
        run: |
          ${pythonLocation}/bin/python -V -V
          ${pythonLocation}/bin/python -m pip list
      - name: Build Application Bundle
        run: ${pythonLocation}/bin/python setup.py ${LITE_FLAG} --dist-dir ${DISTDIR}
      - name: Test Application Bundle
        run: ./test.sh -t 30 -i 5 -d 2 ${DISTDIR}/Spyder.app/Contents/MacOS/Spyder --debug-info=verbose
      - name: Build Disk Image
        run: ${pythonLocation}/bin/python setup.py ${LITE_FLAG} --dist-dir ${DISTDIR} --dmg --no-app
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          path: ${{ env.DISTDIR }}/${{ env.DMGNAME }}
          name: ${{ env.DMGNAME }}
      - name: Get Release
        if: ${{github.event_name == 'release'}}
        id: get_release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        uses: bruceadams/get-release@v1.2.0
      - name: Upload Release Asset
        if: ${{github.event_name == 'release'}}
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.get_release.outputs.upload_url }}
          asset_path: ${{ env.DISTDIR }}/${{ env.DMGNAME }}
          asset_name: ${{ env.DMGNAME }}
          asset_content_type: application/x-apple-diskimage
