# %% sig
  def foo():
# %% body
      spam = 42
      return spam
# %% numpy
      """
      SUMMARY.

      Returns
      -------
      TYPE
          DESCRIPTION.
      """
# %% google
      """SUMMARY.

      Returns:
          spam (TYPE): DESCRIPTION.
      """
# %% sphinx
      """SUMMARY.

      :return: DESCRIPTION
      :rtype: TYPE
      """
